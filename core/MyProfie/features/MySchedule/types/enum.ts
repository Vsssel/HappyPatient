export enum WorkingStatus {
    NOT_WORKING = -1,
    UNAVAILABLE = 0,
    AVAILABLE = 1
};

export enum SlotStatus {
    MISSED = "missed",
    BOOKED = "booked",
    CONFIRMED = "confirmed",
    LUNCH = "lunch"
  }